name: OpenRouter Full Tournament Validation

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow temporarily paused while the full-tournament files are uploaded."
